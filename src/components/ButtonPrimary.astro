---
interface Props { className?: string };

const { className } = Astro.props;
---
<button class=`px-[1.4rem] py-[0.35rem] font-semibold bg-main font-inter rounded-lg text-night border-2 border-main hover:opacity-70 ${className}`>
    <slot />
</button>