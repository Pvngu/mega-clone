---
interface Props { className?: string };

const { className } = Astro.props;
---
<button class=`px-[1.5rem] py-[0.35rem] font-semibold font-main rounded-lg text-main border-2 border-main ${className}`>
    <slot />
</button>